<table class="table table-striped">
  <thead>
    <tr>
    <td>姓名</td>
    <td></td>
    <td>信箱</td>
    <td></td>
    <td>電話</td>
    <td></td>
    <td>推特</td>
    </tr>
  </thead>
  <tbody>
  <% @friends.each do |friend| %>
    <tr>
      <td><%= link_to friend.name, friend_path(friend.id) %></td>
      <td></td>
      <td><%= friend.email %></td>
      <td></td>
      <td><%= friend.tel %></td>
      <td></td>
      <td><%= friend.twitter %></td>
      <td>
        <%= link_to "編輯", edit_friend_path(friend.id) %>
      </td> 
      <td>
        <%= link_to "刪除", friend_path(friend.id), method: "delete" %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to '新增好友', new_friend_path %>
